services:
  id-card-service:
    build: .
    ports:
      - "8085:8085"
    volumes:
      - /opt/pw-browsers:/opt/pw-browsers
    environment:
      - PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers
    restart: unless-stopped